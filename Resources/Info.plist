<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Resolve the bundled multiresolution icon consistently in Finder, the Dock, and app windows. -->
    <key>CFBundleIconFile</key>
    <string>AppIcon</string>
    <!-- Users supply arbitrary appliance hosts and can explicitly choose HTTP. DeviceTransport still
         validates every HTTPS certificate normally or against a specifically approved device pin. -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsArbitraryLoads</key>
        <true/>
    </dict>
    <!-- Keep version metadata explicit for the locally signed app and future distribution builds. -->
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
</dict>
</plist>
